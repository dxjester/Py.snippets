# convert list of values to a dictionary utilizing index
def compress_vector(x):
    d = {'inds': [], 'vals': []}
    for loc, value in enumerate(x):
        if value != 0.0:
            d['inds'].append()
            d['vals'].append(val)

    return d
